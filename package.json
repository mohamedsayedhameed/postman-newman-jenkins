{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "project for run test by newman",
  "directories": {
    "api-tests-production": "new man run/tests/newman-run.postman_collection.json"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sayed",
  "license": "ISC",
  "dependencies": {
    "newman": "^5.3.2"
  }
}
